# 📦 GitHub Actions Artifacts Explained

Share files between jobs and download build outputs!

---

## 🎯 Telugu Simple ga

> "VM destroy ayyina files survive avuthayi!"
> "Job 1 nundi Job 2 ki files pass cheyyochu!"

---

## 🤔 The Problem

```
JOB 1 (Build):                     JOB 2 (Docker):
─────────────                      ────────────────
┌────────────────────┐             ┌────────────────────┐
│ Runner VM #1       │             │ Runner VM #2       │
│                    │             │                    │
│ mvn package        │             │ docker build       │
│    ↓               │             │                    │
│ target/app.jar ✅  │  💀 ──────▶│ ??? Where's JAR?  │
│                    │             │                    │
└────────────────────┘             └────────────────────┘
    VM DESTROYED!                     FRESH VM!

Problem: Job 1 lo built JAR, Job 2 ki ela transfer?
```

---

## 💡 The Solution: Artifacts

```
                          GITHUB CLOUD
                    ┌───────────────────────────┐
                    │                           │
                    │    ARTIFACT STORAGE        │
                    │                           │
                    │    📦 todo-backend-jar    │
JOB 1:              │       └── app.jar         │           JOB 2:
┌──────────────┐    │                           │    ┌──────────────┐
│ mvn package  │    │    Stored for 7 days      │    │ docker build │
│    ↓         │    │                           │    │    ↑         │
│ target/*.jar │    └───────────────────────────┘    │ target/*.jar │
│    ↓         │              ▲            │         │              │
│ UPLOAD ──────┼──────────────┘            └─────────┼── DOWNLOAD   │
│              │                                     │              │
└──────────────┘                                     └──────────────┘
   VM #1 dies              FILES SURVIVE!               VM #2 gets
   after job                                            the files!
```

---

## 📤 How to Upload Artifacts

```yaml
# Job 1: Build and Upload
- name: 📦 Build JAR
  run: mvn package -DskipTests

- name: 📤 Upload JAR Artifact
  uses: actions/upload-artifact@v4
  with:
    name: todo-backend-jar          # Name to identify
    path: ./target/*.jar            # What to upload
    retention-days: 7               # How long to keep
```

---

## 📥 How to Download Artifacts

```yaml
# Job 2: Download and Use
- name: 📥 Download JAR Artifact
  uses: actions/download-artifact@v4
  with:
    name: todo-backend-jar           # Same name as upload!
    path: ./target/                  # Where to download

- name: 🐳 Build Docker Image
  run: docker build -t myapp .
  # Dockerfile can now COPY target/*.jar!
```

---

## 📍 Where to Find Artifacts

```
Method 1: Workflow Run Summary Page
───────────────────────────────────
github.com/<user>/<repo>/actions
→ Click on workflow run
→ Scroll down to "Artifacts" section

┌──────────────────────────────────────────────────────────────┐
│ Artifacts                                     Produced during│
│ ──────────────────────────────────────────────────────────  │
│ 📦 todo-backend-jar                           43 MB         │
│ 📊 test-results                               9.94 KB       │
│                                                              │
│                                    [Download]  [Download All]│
└──────────────────────────────────────────────────────────────┘
```

---

## 📊 Artifacts vs GHCR vs Cache

```
┌──────────────────┬────────────────────┬─────────────────────┐
│    ARTIFACTS     │       GHCR         │       CACHE         │
├──────────────────┼────────────────────┼─────────────────────┤
│ Purpose:         │ Purpose:           │ Purpose:            │
│ Share files      │ Docker images      │ Speed up builds     │
│ between jobs     │ for deployment     │                     │
├──────────────────┼────────────────────┼─────────────────────┤
│ Location:        │ Location:          │ Location:           │
│ Workflow run     │ Profile →          │ Repo → Actions →    │
│ summary page     │ Packages           │ Caches              │
├──────────────────┼────────────────────┼─────────────────────┤
│ Lifetime:        │ Lifetime:          │ Lifetime:           │
│ 7-90 days        │ Permanent          │ 7 days unused       │
├──────────────────┼────────────────────┼─────────────────────┤
│ Uploaded by:     │ Pushed by:         │ Saved by:           │
│ upload-artifact  │ docker push        │ cache action        │
│ action           │ build-push-action  │ setup-java cache    │
├──────────────────┼────────────────────┼─────────────────────┤
│ Example use:     │ Example use:       │ Example use:        │
│ JAR, test        │ nginx:1.0          │ ~/.m2/repository    │
│ reports, logs    │ todo-backend:abc   │ node_modules        │
└──────────────────┴────────────────────┴─────────────────────┘
```

---

## 🔄 Complete Flow Diagram

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                         GITHUB ACTIONS WORKFLOW                                  │
│                                                                                  │
│  ┌──────────────────────────────────────────────────────────────────────────┐   │
│  │ JOB 1: Build & Test                                                      │   │
│  │                                                                          │   │
│  │ Runner VM #1                                                             │   │
│  │ ┌────────────────────────────────────────────────────────────────────┐  │   │
│  │ │ checkout → setup-java → mvn package → target/app.jar              │  │   │
│  │ └───────────────────────────────────────────────┬────────────────────┘  │   │
│  │                                                 │                        │   │
│  │                                    upload-artifact                       │   │
│  │                                                 │                        │   │
│  └─────────────────────────────────────────────────┼────────────────────────┘   │
│                                                    │                             │
│                                                    ▼                             │
│                         ┌──────────────────────────────────────────────────┐    │
│                         │           GITHUB CLOUD STORAGE                    │    │
│                         │                                                   │    │
│                         │  Artifacts:                                       │    │
│                         │  📦 todo-backend-jar (43 MB)                     │    │
│                         │  📊 test-results (10 KB)                         │    │
│                         │                                                   │    │
│                         │  Retention: 7 days                               │    │
│                         │  Downloadable from UI!                           │    │
│                         │                                                   │    │
│                         └───────────────────────────┬──────────────────────┘    │
│                                                     │                            │
│  ┌──────────────────────────────────────────────────┼───────────────────────┐   │
│  │ JOB 2: Docker Build                              │                       │   │
│  │                                                  │                       │   │
│  │ Runner VM #2 (NEW!)                download-artifact                     │   │
│  │ ┌────────────────────────────────────────────────▼───────────────────┐  │   │
│  │ │ checkout → download JAR → docker build → push to GHCR             │  │   │
│  │ └────────────────────────────────────────────────────────────────────┘  │   │
│  │                                                                          │   │
│  └──────────────────────────────────────────────────────────────────────────┘   │
│                                                                                  │
└─────────────────────────────────────────────────────────────────────────────────┘
```

---

## 🎬 Telugu Summary

```
Problem:
────────
Jobs veru veru VMs lo run avthayi
Job 1 VM destroy ayyaka files pothayi
Job 2 ki files ela isthav?

Solution - Artifacts:
─────────────────────
Job 1: upload-artifact → GitHub Cloud ki upload
       VM destroy ayyina files safe!

Job 2: download-artifact → GitHub Cloud nundi download
       Fresh VM lo files vasthay!

Key Points:
───────────
• actions/upload-artifact@v4 - Upload files
• actions/download-artifact@v4 - Download files
• name: "same-name" - Both actions lo SAME name undali
• retention-days: 7 - 7 days tarvata auto-delete
• Workflow run page lo download button untundi
```

---

**Next: [03-build-optimization.md](./03-build-optimization.md)** 👉
