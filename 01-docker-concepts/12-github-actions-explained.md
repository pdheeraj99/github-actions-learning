# 🚀 12 - GitHub Actions Complete Explanation

## 1. What is GitHub Actions? (Basics First!)

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                                                                              │
│   GITHUB ACTIONS = FREE CI/CD TOOL built into GitHub!                       │
│   ═══════════════════════════════════════════════════                        │
│                                                                              │
│   CI = Continuous Integration (automatically test/build code)               │
│   CD = Continuous Deployment (automatically deploy to servers)              │
│                                                                              │
│   ════════════════════════════════════════════════════════════════════════  │
│                                                                              │
│   Telugu lo Simple ga:                                                       │
│   ─────────────────────                                                      │
│   "Nuvvu code push chesthe, GitHub AUTOMATICALLY:                           │
│    • Tests run chesthundhi                                                   │
│    • Docker image build chesthundhi                                          │
│    • Registry ki push chesthundhi                                           │
│    • Servers ki deploy chesthundhi!"                                        │
│                                                                              │
│   Nuvvu emi cheyakkarledu - AUTOMATIC! That's CI/CD! 🎯                     │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 2. Key Terminology (Gurthu Pettuko!)

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                                                                              │
│   GITHUB ACTIONS TERMINOLOGY:                                                │
│   ═══════════════════════════                                                │
│                                                                              │
│   1. WORKFLOW (ఒక పెద్ద ప్లాన్)                                               │
│      └── .github/workflows/ folder lo unna YAML file                        │
│      └── Entire automation script                                           │
│      └── Example: docker-build.yml                                          │
│                                                                              │
│   2. EVENT (ట్రిగ్గర్)                                                        │
│      └── When should workflow run?                                          │
│      └── Examples: push, pull_request, schedule, manual                     │
│      └── "Code push aithe RUN cheyyi!"                                      │
│                                                                              │
│   3. JOB (ఒక పెద్ద టాస్క్)                                                    │
│      └── A group of steps that run on SAME runner                           │
│      └── Multiple jobs can run in PARALLEL!                                 │
│      └── Example: "build-and-push" job                                      │
│                                                                              │
│   4. STEP (ఒక చిన్న స్టెప్)                                                   │
│      └── One action/command inside a job                                    │
│      └── Steps run SEQUENTIALLY (one after another)                         │
│      └── Example: "Checkout code", "Build Docker"                           │
│                                                                              │
│   5. RUNNER (GitHub's Computer)                                              │
│      └── The VM where your workflow runs                                    │
│      └── GitHub provides FREE runners!                                      │
│      └── ubuntu-latest, windows-latest, macos-latest                        │
│                                                                              │
│   6. ACTION (Pre-built tool)                                                 │
│      └── Reusable code someone else wrote                                   │
│      └── Like npm packages for CI/CD!                                       │
│      └── Example: actions/checkout@v4, docker/build-push-action@v5          │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 3. Visual: How It All Connects

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                                                                              │
│   HIERARCHY (Top to Bottom):                                                 │
│   ══════════════════════════                                                 │
│                                                                              │
│   📁 Repository                                                              │
│   └── 📁 .github/workflows/                                                  │
│       └── 📄 docker-build.yml  ← WORKFLOW                                    │
│           │                                                                  │
│           ├── on: push        ← EVENT (trigger)                             │
│           │                                                                  │
│           └── jobs:                                                          │
│               └── build-and-push:  ← JOB                                    │
│                   │                                                          │
│                   ├── runs-on: ubuntu-latest  ← RUNNER                      │
│                   │                                                          │
│                   └── steps:                                                 │
│                       ├── Step 1: Checkout ← STEP (uses ACTION)             │
│                       ├── Step 2: Login    ← STEP                           │
│                       ├── Step 3: Build    ← STEP                           │
│                       └── Step 4: Push     ← STEP                           │
│                                                                              │
│   ════════════════════════════════════════════════════════════════════════  │
│                                                                              │
│   Telugu:                                                                    │
│   ────────                                                                   │
│   WORKFLOW = Full plan                                                       │
│   JOB = Oka phase of plan (build phase, deploy phase)                       │
│   STEP = Individual task in that phase                                      │
│   RUNNER = Computer that executes                                           │
│   ACTION = Ready-made tool we use                                           │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 4. YAML File Structure - Line by Line

### 4.1 File Location (CHALA IMPORTANT!)

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                                                                              │
│   WHERE SHOULD WORKFLOW FILE BE?                                             │
│   ══════════════════════════════                                             │
│                                                                              │
│   your-repo/                                                                 │
│   └── .github/                    ← Hidden folder (. tho start)             │
│       └── workflows/              ← Must be exactly "workflows"             │
│           └── docker-build.yml    ← Your workflow file (.yml or .yaml)     │
│                                                                              │
│   RULES:                                                                     │
│   ─────────                                                                  │
│   ✅ .github/workflows/anything.yml     ← Works!                            │
│   ✅ .github/workflows/ci.yaml          ← Works!                            │
│   ❌ github/workflows/ci.yml            ← NO! Missing dot!                  │
│   ❌ .github/workflow/ci.yml            ← NO! "workflow" not "workflows"!   │
│   ❌ workflows/ci.yml                   ← NO! Not inside .github!           │
│                                                                              │
│   Telugu:                                                                    │
│   ────────                                                                   │
│   "Exact path lo pettali - .github/workflows/ lo YML files maatrame         │
│    GitHub automatically detect chesthundhi!"                                │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 5. Our Workflow - Block by Block Explanation

### 5.1 NAME Section

```yaml
name: 🐳 React Docker Build
```

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                                                                              │
│   name: 🐳 React Docker Build                                                │
│   ═══════════════════════════                                                │
│                                                                              │
│   Eemi Chesthundhi?                                                          │
│   ──────────────────                                                         │
│   • GitHub Actions tab lo ee name kanipistundhi                             │
│   • Workflow identify cheyyadaniki                                          │
│                                                                              │
│   GitHub UI lo:                                                              │
│   ┌────────────────────────────────────────────────────────────────────┐    │
│   │ Actions                                                             │    │
│   │ ├── 🐳 React Docker Build    ← THIS NAME!                          │    │
│   │ │   ├── Run #1 ✅                                                   │    │
│   │ │   └── Run #2 🔄                                                   │    │
│   │ └── Another Workflow                                                │    │
│   └────────────────────────────────────────────────────────────────────┘    │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

### 5.2 ON Section (TRIGGER - Chala Important!)

```yaml
on:
  push:
    branches: [main]
    paths:
      - '03-react-pipeline/todo-frontend/**'
  pull_request:
    branches: [main]
    paths:
      - '03-react-pipeline/todo-frontend/**'
  workflow_dispatch:
```

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                                                                              │
│   ON: WHEN SHOULD THIS WORKFLOW RUN?                                        │
│   ══════════════════════════════════                                         │
│                                                                              │
│   ────────────────────────────────────────────────────────────────────────  │
│                                                                              │
│   1. on: push                                                                │
│      branches: [main]                                                        │
│      ──────────────────                                                      │
│      "main branch ki push aithe RUN cheyyi"                                 │
│                                                                              │
│      Multiple branches:                                                      │
│      branches: [main, develop, feature/*]                                   │
│      "main, develop, or any feature/* branch ki push aithe"                │
│                                                                              │
│   ────────────────────────────────────────────────────────────────────────  │
│                                                                              │
│   2. paths:                                                                  │
│      - '03-react-pipeline/todo-frontend/**'                                 │
│      ──────────────────────────────────────                                  │
│      "ONLY ee folder lo changes unte RUN cheyyi!"                           │
│                                                                              │
│      ** = any files, any depth                                              │
│      * = any files, same directory only                                     │
│                                                                              │
│      BENEFIT:                                                                │
│      README.md change chesthe workflow RUN AVVADHU! ⚡                       │
│      React code change chesthe only appudu run avuthundhi!                  │
│      SAVES CI MINUTES! 💰                                                    │
│                                                                              │
│   ────────────────────────────────────────────────────────────────────────  │
│                                                                              │
│   3. pull_request:                                                           │
│      ────────────────                                                        │
│      PR create chesthe or update chesthe RUN cheyyi                         │
│      Code review mundu test cheyyadaniki                                    │
│                                                                              │
│   ────────────────────────────────────────────────────────────────────────  │
│                                                                              │
│   4. workflow_dispatch:                                                      │
│      ──────────────────                                                      │
│      "MANUAL trigger button add cheyyi!"                                    │
│                                                                              │
│      GitHub UI lo:                                                           │
│      ┌─────────────────────────────────────────────────────────────────┐   │
│      │ Actions → 🐳 React Docker Build                                  │   │
│      │                                                                  │   │
│      │ [Run workflow ▼]  ← THIS BUTTON!                                │   │
│      └─────────────────────────────────────────────────────────────────┘   │
│                                                                              │
│      Nuvvu manually run cheyachu!                                           │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

### 5.3 ENV Section (Environment Variables)

```yaml
env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}/todo-frontend
```

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                                                                              │
│   ENV: GLOBAL VARIABLES                                                      │
│   ═════════════════════                                                      │
│                                                                              │
│   REGISTRY: ghcr.io                                                          │
│   ──────────────────                                                         │
│   • ghcr.io = GitHub Container Registry                                     │
│   • Docker images store chese place                                         │
│   • Alternative: docker.io (Docker Hub), ecr.aws (AWS)                      │
│                                                                              │
│   ════════════════════════════════════════════════════════════════════════  │
│                                                                              │
│   IMAGE_NAME: ${{ github.repository }}/todo-frontend                        │
│   ───────────────────────────────────────────────────                        │
│                                                                              │
│   ${{ ... }} = EXPRESSION SYNTAX                                            │
│                                                                              │
│   github.repository = "pdheeraj99/github-actions-learning"                  │
│   So IMAGE_NAME = "pdheeraj99/github-actions-learning/todo-frontend"        │
│                                                                              │
│   Final image path:                                                          │
│   ghcr.io/pdheeraj99/github-actions-learning/todo-frontend                  │
│                                                                              │
│   ════════════════════════════════════════════════════════════════════════  │
│                                                                              │
│   Telugu:                                                                    │
│   ────────                                                                   │
│   "Repo pere dynamic ga teskuntunnam - hardcode avasaram ledu!"             │
│   "Ee workflow vere repo ki copy chesina work avuthundhi!"                  │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

### 5.4 JOBS Section

```yaml
jobs:
  build-and-push:
    name: 🏗️ Build & Push Docker Image
    runs-on: ubuntu-latest
```

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                                                                              │
│   JOBS: THE ACTUAL WORK!                                                     │
│   ══════════════════════                                                     │
│                                                                              │
│   jobs:                                                                      │
│     build-and-push:        ← Job ID (internal name)                         │
│       name: 🏗️ Build...    ← Display name in UI                             │
│                                                                              │
│   ════════════════════════════════════════════════════════════════════════  │
│                                                                              │
│   runs-on: ubuntu-latest                                                     │
│   ─────────────────────────                                                  │
│                                                                              │
│   "Ee job EKKADA run avvali?"                                               │
│                                                                              │
│   Options:                                                                   │
│   ┌──────────────────────────────────────────────────────────────────────┐  │
│   │ runs-on: ubuntu-latest    ← Most common! Docker works best here!    │  │
│   │ runs-on: ubuntu-22.04     ← Specific Ubuntu version                 │  │
│   │ runs-on: windows-latest   ← Windows runner                          │  │
│   │ runs-on: macos-latest     ← macOS runner (for iOS builds)          │  │
│   └──────────────────────────────────────────────────────────────────────┘  │
│                                                                              │
│   Telugu:                                                                    │
│   ────────                                                                   │
│   "GitHub, naku oka Ubuntu VM ichhi - akkada Docker run chesthanu!"         │
│                                                                              │
│   Remember from 11-github-runners.md:                                        │
│   Ee VM GitHub's cloud lo untundhi!                                         │
│   Nee PC NOT INVOLVED!                                                       │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

### 5.5 PERMISSIONS Section

```yaml
permissions:
  contents: read
  packages: write
```

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                                                                              │
│   PERMISSIONS: WHAT CAN THIS WORKFLOW DO?                                   │
│   ════════════════════════════════════════                                   │
│                                                                              │
│   contents: read                                                             │
│   ──────────────                                                             │
│   • Repo code READ cheyachu                                                 │
│   • Commit/push KAADU (write ledu)                                          │
│                                                                              │
│   packages: write                                                            │
│   ────────────────                                                           │
│   • GHCR ki images PUSH cheyachu ✅                                          │
│   • Idi lekapothe GHCR push fail avuthundhi!                                │
│                                                                              │
│   ════════════════════════════════════════════════════════════════════════  │
│                                                                              │
│   Security Best Practice:                                                    │
│   ────────────────────────                                                   │
│   MINIMUM permissions ivvu - kavalsindhe kavalsindhe!                       │
│                                                                              │
│   Other permissions available:                                               │
│   • actions: read/write                                                      │
│   • issues: write (issue create/comment)                                    │
│   • pull-requests: write (PR comment)                                       │
│   • deployments: write                                                       │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

### 5.6 STEPS Section - Each Step Explained

#### Step 1: Checkout

```yaml
- name: 📥 Checkout Code
  uses: actions/checkout@v4
```

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                                                                              │
│   STEP 1: CHECKOUT CODE                                                      │
│   ═════════════════════                                                      │
│                                                                              │
│   uses: actions/checkout@v4                                                  │
│   ─────────────────────────                                                  │
│                                                                              │
│   "uses" = Pre-built action use chesthunnam                                 │
│   "actions/checkout" = Official GitHub action                               │
│   "@v4" = Version 4                                                          │
│                                                                              │
│   ════════════════════════════════════════════════════════════════════════  │
│                                                                              │
│   EEMI CHESTHUNDHI?                                                          │
│   ──────────────────                                                         │
│                                                                              │
│   Runner starts with EMPTY directory!                                        │
│                                                                              │
│   BEFORE checkout:                                                           │
│   /home/runner/work/github-actions-learning/                                │
│   └── (EMPTY!)                                                               │
│                                                                              │
│   AFTER checkout:                                                            │
│   /home/runner/work/github-actions-learning/                                │
│   ├── .github/workflows/                                                    │
│   ├── 01-concepts/                                                           │
│   ├── 03-react-pipeline/                                                    │
│   │   └── todo-frontend/                                                    │
│   │       ├── Dockerfile                                                    │
│   │       ├── package.json                                                  │
│   │       └── ...                                                            │
│   └── ...                                                                    │
│                                                                              │
│   Telugu:                                                                    │
│   ────────                                                                   │
│   "Nee repo code ni Runner machine ki clone chesthundhi!"                   │
│   "git clone" internally run avuthundhi!                                    │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

#### Step 2: Login to GHCR

```yaml
- name: 🔐 Login to GHCR
  uses: docker/login-action@v3
  with:
    registry: ${{ env.REGISTRY }}
    username: ${{ github.actor }}
    password: ${{ secrets.GITHUB_TOKEN }}
```

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                                                                              │
│   STEP 2: LOGIN TO CONTAINER REGISTRY                                       │
│   ═══════════════════════════════════                                        │
│                                                                              │
│   docker/login-action@v3                                                     │
│   ──────────────────────                                                     │
│   Docker team create chesina official action                                │
│   Registry ki login chesthundhi                                             │
│                                                                              │
│   ════════════════════════════════════════════════════════════════════════  │
│                                                                              │
│   with:                     ← "with" = Inputs for the action                │
│     registry: ghcr.io       ← Container Registry URL                        │
│     username: github.actor  ← Nee GitHub username                           │
│     password: secrets.GITHUB_TOKEN  ← 🔑 THE MAGIC!                         │
│                                                                              │
│   ════════════════════════════════════════════════════════════════════════  │
│                                                                              │
│   🔑 GITHUB_TOKEN SECRET:                                                    │
│   ────────────────────────                                                   │
│   • GitHub AUTOMATICALLY provides ee secret!                                │
│   • Nuvvu create avasaram LEDU!                                             │
│   • Every workflow run ki fresh token vasthundhi                            │
│   • GHCR push permission already untundhi                                   │
│                                                                              │
│   Telugu:                                                                    │
│   ────────                                                                   │
│   "GitHub automatic ga oka password ichindhi.                               │
│    Neeku emi setup cheyakkarledu - out of box work avuthundhi!"             │
│                                                                              │
│   ════════════════════════════════════════════════════════════════════════  │
│                                                                              │
│   Internally runs:                                                           │
│   docker login ghcr.io -u pdheeraj99 -p ***                                 │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

#### Step 3: Setup Buildx

```yaml
- name: 🔧 Setup Docker Buildx
  uses: docker/setup-buildx-action@v3
```

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                                                                              │
│   STEP 3: SETUP BUILDX                                                       │
│   ════════════════════                                                       │
│                                                                              │
│   Docker Buildx = MODERN Docker builder                                     │
│   ───────────────────────────────────                                        │
│                                                                              │
│   Benefits:                                                                  │
│   • Better caching                                                           │
│   • Multi-platform builds (linux/amd64, linux/arm64)                        │
│   • BuildKit features (parallel, efficient)                                 │
│                                                                              │
│   Telugu:                                                                    │
│   ────────                                                                   │
│   "Normal docker build kaadu - UPGRADED version setup chesthunnam!"         │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

#### Step 4: Extract Metadata

```yaml
- name: 📋 Extract Metadata
  id: meta
  uses: docker/metadata-action@v5
  with:
    images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
    tags: |
      type=sha,prefix=
      type=raw,value=latest,enable=${{ github.ref == 'refs/heads/main' }}
```

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                                                                              │
│   STEP 4: EXTRACT METADATA (Tags & Labels)                                  │
│   ═════════════════════════════════════════                                  │
│                                                                              │
│   id: meta                                                                   │
│   ─────────                                                                  │
│   • Ee step ki "meta" ani ID ichham                                         │
│   • Later steps lo ${{ steps.meta.outputs.xxx }} tho use cheyyachu         │
│                                                                              │
│   ════════════════════════════════════════════════════════════════════════  │
│                                                                              │
│   tags: |                                                                    │
│     type=sha,prefix=                                                         │
│     type=raw,value=latest,enable=${{ github.ref == 'refs/heads/main' }}     │
│   ─────────────────────────────────────────────────────────────────────────  │
│                                                                              │
│   tag type=sha,prefix=                                                       │
│   ────────────────────                                                       │
│   Git commit SHA use chesthundhi                                            │
│   Example: ghcr.io/pdheeraj99/.../todo-frontend:8b9f323                     │
│                                 └────────────────────────                    │
│                                    Git short SHA!                           │
│                                                                              │
│   type=raw,value=latest,enable=...                                          │
│   ─────────────────────────────────                                          │
│   main branch aithe "latest" tag add cheyyi                                 │
│   Example: ghcr.io/.../todo-frontend:latest                                 │
│                                                                              │
│   ════════════════════════════════════════════════════════════════════════  │
│                                                                              │
│   Telugu:                                                                    │
│   ────────                                                                   │
│   "Docker image ki oka tag kavali - 8b9f323 lanti di.                       │
│    Ee step aa tag prepare chesthundhi."                                     │
│                                                                              │
│   Your image has TWO tags:                                                   │
│   • todo-frontend:8b9f323 (commit specific)                                 │
│   • todo-frontend:latest (always latest main)                               │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

#### Step 5: Build and Push

```yaml
- name: 🐳 Build and Push
  uses: docker/build-push-action@v5
  with:
    context: ./03-react-pipeline/todo-frontend
    push: ${{ github.event_name != 'pull_request' }}
    tags: ${{ steps.meta.outputs.tags }}
    labels: ${{ steps.meta.outputs.labels }}
    cache-from: type=gha
    cache-to: type=gha,mode=max
```

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                                                                              │
│   STEP 5: BUILD AND PUSH! (The Main Event! 🎯)                              │
│   ════════════════════════════════════════════                               │
│                                                                              │
│   context: ./03-react-pipeline/todo-frontend                                │
│   ─────────────────────────────────────────────                              │
│   • Dockerfile ekkada undhi?                                                │
│   • Build context = ee folder                                               │
│   • Dockerfile ee folder lo search chesthundhi                              │
│                                                                              │
│   ════════════════════════════════════════════════════════════════════════  │
│                                                                              │
│   push: ${{ github.event_name != 'pull_request' }}                          │
│   ────────────────────────────────────────────────                           │
│                                                                              │
│   ❓ github.event_name != 'pull_request'                                     │
│                                                                              │
│   If push event:     event_name = "push"        → push = TRUE ✅            │
│   If PR event:       event_name = "pull_request" → push = FALSE ❌          │
│                                                                              │
│   WHY?                                                                       │
│   PR lo image push cheyamu - just build test chestharu                      │
│   main ki merge aithe then push!                                            │
│                                                                              │
│   ════════════════════════════════════════════════════════════════════════  │
│                                                                              │
│   tags: ${{ steps.meta.outputs.tags }}                                      │
│   ─────────────────────────────────────                                      │
│   Previous step (id: meta) nunchi tags teskuntunnam                         │
│                                                                              │
│   ════════════════════════════════════════════════════════════════════════  │
│                                                                              │
│   cache-from: type=gha                                                       │
│   cache-to: type=gha,mode=max                                               │
│   ───────────────────────────                                                │
│                                                                              │
│   gha = GitHub Actions cache                                                │
│                                                                              │
│   • Previous build layers cache lo store avuthayi                           │
│   • Next build appudu reuse avuthayi                                        │
│   • npm ci skip avuthundhi if package.json same!                            │
│   • 5 mins → 1 min! ⚡                                                       │
│                                                                              │
│   Telugu:                                                                    │
│   ────────                                                                   │
│   "Layer caching mana local Docker Desktop laga -                           │
│    but GitHub Actions lo! Next build fast avuthundhi!"                      │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 6. The Complete Flow - Visual Timeline

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                                                                              │
│   TIMELINE: What happens when you push?                                     │
│   ══════════════════════════════════════                                     │
│                                                                              │
│   12:40 PM - You: git push                                                  │
│        │                                                                     │
│   12:40:01 - GitHub receives push                                           │
│        │                                                                     │
│   12:40:02 - GitHub checks: ".github/workflows/*.yml unda?"                 │
│        │     YES! docker-build.yml found!                                   │
│        │                                                                     │
│   12:40:03 - GitHub checks: "on: push matches?"                             │
│        │     Branch: main ✅                                                 │
│        │     Path: 03-react-pipeline/todo-frontend/** ✅                    │
│        │                                                                     │
│   12:40:05 - GitHub: "Oka Ubuntu runner assign chesta!"                     │
│        │     Runner VM starts booting...                                    │
│        │                                                                     │
│   12:40:15 - Runner ready! Job starts!                                      │
│        │                                                                     │
│   12:40:20 - Step 1: Checkout (git clone chesthundhi)                       │
│        │                                                                     │
│   12:40:25 - Step 2: Login to GHCR                                          │
│        │                                                                     │
│   12:40:30 - Step 3: Setup Buildx                                           │
│        │                                                                     │
│   12:40:35 - Step 4: Extract metadata                                       │
│        │                                                                     │
│   12:40:40 - Step 5: docker build starts!                                   │
│        │     ├── FROM node:20-alpine (pulling...)                           │
│        │     ├── npm ci (installing deps...)                                │
│        │     ├── npm run build (building...)                                │
│        │     └── FROM nginx:alpine (final stage)                            │
│        │                                                                     │
│   12:43:00 - Build complete! Pushing to GHCR...                             │
│        │                                                                     │
│   12:43:30 - ✅ Image pushed to ghcr.io/pdheeraj99/.../todo-frontend       │
│        │                                                                     │
│   12:43:32 - Runner cleanup & destroy                                       │
│        │                                                                     │
│   12:43:35 - You get ✅ notification!                                        │
│                                                                              │
│   ════════════════════════════════════════════════════════════════════════  │
│                                                                              │
│   TOTAL TIME: ~3 minutes                                                     │
│   YOUR WORK: Just git push!                                                  │
│   GITHUB'S WORK: Everything else! 🎉                                        │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 🎯 Summary

| Component | Purpose (Telugu) |
|-----------|-----------------|
| `name` | Workflow name - UI lo display |
| `on` | WHEN to run - push, PR, manual |
| `on.paths` | WHICH folders change aithe run |
| `env` | Global variables |
| `jobs` | Major work sections |
| `runs-on` | WHICH machine - ubuntu/windows/mac |
| `permissions` | WHAT can workflow do |
| `steps` | Individual tasks |
| `uses` | Pre-built action use |
| `with` | Inputs to action |
| `secrets.GITHUB_TOKEN` | Auto-provided auth token |

---

## 👉 Next: More concepts coming in next files
