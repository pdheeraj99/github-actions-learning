# 📊 JaCoCo - Code Coverage for Java

Measure how much of your code is tested!

---

## 🎯 Telugu Simple ga

> "JaCoCo = Test lo nee code entha cover ayyindho measure chesthundhi!"
> "80% coverage ante 100 lines lo 80 lines test lo run ayyayi!"

---

## 🤔 What is Code Coverage?

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                         CODE COVERAGE EXPLAINED                                  │
│                                                                                  │
│   Your Code:                           Your Tests:                              │
│   ──────────                           ────────────                              │
│   public class Calculator {            @Test                                    │
│     public int add(int a, int b) {     void testAdd() {                         │
│       return a + b;  ← TESTED ✅         assertEquals(5, calc.add(2, 3));       │
│     }                                  }                                         │
│                                                                                  │
│     public int subtract(int a, int b){ // No test for subtract! ❌              │
│       return a - b;  ← NOT TESTED ❌                                            │
│     }                                                                            │
│   }                                                                              │
│                                                                                  │
│   ════════════════════════════════════════════════════════════════════════════  │
│                                                                                  │
│   COVERAGE = Lines tested / Total lines                                         │
│            = 1 method / 2 methods                                               │
│            = 50%                                                                │
│                                                                                  │
└─────────────────────────────────────────────────────────────────────────────────┘
```

---

## 📦 JaCoCo in pom.xml

```xml
<build>
  <plugins>
    <!-- JaCoCo Plugin -->
    <plugin>
      <groupId>org.jacoco</groupId>
      <artifactId>jacoco-maven-plugin</artifactId>
      <version>0.8.11</version>
      <executions>
        <!-- Prepare coverage agent before tests -->
        <execution>
          <id>prepare-agent</id>
          <goals>
            <goal>prepare-agent</goal>
          </goals>
        </execution>
        
        <!-- Generate report after tests -->
        <execution>
          <id>report</id>
          <phase>test</phase>
          <goals>
            <goal>report</goal>
          </goals>
        </execution>
      </executions>
    </plugin>
  </plugins>
</build>
```

---

## 🔄 How JaCoCo Works

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                         JACOCO FLOW                                              │
│                                                                                  │
│   mvn test                                                                       │
│       │                                                                          │
│       ▼                                                                          │
│   ┌──────────────────────────────────────────────────────────────────────────┐  │
│   │  1. prepare-agent                                                         │  │
│   │     JaCoCo attaches to JVM                                               │  │
│   │     Watches which lines get executed                                      │  │
│   └──────────────────────────────────────────────────────────────────────────┘  │
│       │                                                                          │
│       ▼                                                                          │
│   ┌──────────────────────────────────────────────────────────────────────────┐  │
│   │  2. Tests Run                                                             │  │
│   │     JaCoCo records: "Line 10 executed, Line 15 executed..."              │  │
│   └──────────────────────────────────────────────────────────────────────────┘  │
│       │                                                                          │
│       ▼                                                                          │
│   ┌──────────────────────────────────────────────────────────────────────────┐  │
│   │  3. report                                                                │  │
│   │     Generates: target/site/jacoco/jacoco.xml                             │  │
│   │     HTML report: target/site/jacoco/index.html                           │  │
│   └──────────────────────────────────────────────────────────────────────────┘  │
│                                                                                  │
└─────────────────────────────────────────────────────────────────────────────────┘
```

---

## 🔗 JaCoCo + SonarCloud

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                                                                                  │
│   JaCoCo = MEASURES coverage (creates report)                                   │
│   SonarCloud = READS report and shows nice dashboard                            │
│                                                                                  │
│   ════════════════════════════════════════════════════════════════════════════  │
│                                                                                  │
│   ┌───────────┐         ┌───────────────────┐         ┌───────────────────┐    │
│   │  mvn test │  ────▶  │  JaCoCo Report    │  ────▶  │  SonarCloud       │    │
│   │           │         │  (jacoco.xml)     │         │  Dashboard        │    │
│   └───────────┘         └───────────────────┘         └───────────────────┘    │
│                                                                                  │
│   Workflow:                                                                      │
│   ─────────                                                                      │
│   -Dsonar.coverage.jacoco.xmlReportPaths=target/site/jacoco/jacoco.xml          │
│                                                                                  │
│   This tells SonarCloud: "JaCoCo report ikkada undhi, chadhuvu!"                │
│                                                                                  │
└─────────────────────────────────────────────────────────────────────────────────┘
```

---

## 📊 Coverage Thresholds

| Coverage | Status | Meaning |
|----------|--------|---------|
| 80%+ | ✅ Good | Well-tested code |
| 60-80% | ⚠️ OK | Needs more tests |
| < 60% | ❌ Low | Risk of bugs |

---

## 🎬 Telugu Summary

```
JaCoCo = Java Code Coverage

What it does:
─────────────
• Tests run avvutunte observe chesthundhi
• Enni lines execute ayyayo count chesthundhi
• Report generate chesthundhi (XML, HTML)

SonarCloud tho:
───────────────
• JaCoCo report SonarCloud ki ichchi
• SonarCloud meeru dashboard lo chupisthadhi
• "Coverage 75%" ani display avuthundhi

pom.xml lo add cheyyi:
──────────────────────
• jacoco-maven-plugin
• prepare-agent + report goals
```

---

**Related: [SonarCloud Setup](../../../.github/workflows/spring-boot-build.yml)** 👉
