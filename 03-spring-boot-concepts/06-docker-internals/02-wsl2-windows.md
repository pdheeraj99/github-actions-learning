# 🪟 WSL 2 & Hyper-V: Docker on Windows

How does Docker run Linux containers on Windows?

---

## 🎯 Telugu Simple ga

> "Windows lo Linux run cheyali - adi WSL 2!"
> "WSL 2 run avvadaniki Hyper-V kavali!"

---

## 🤔 The Problem

```
Docker containers need LINUX kernel features:
• namespaces (isolation)
• cgroups (resource limits)
• overlayfs (layered filesystem)

Windows kernel DOESN'T have these! ❌

So how does Docker work on Windows? 🤔
```

---

## 💡 The Solution: WSL 2

```
WSL = Windows Subsystem for Linux

WSL 2 = Version 2 (uses real Linux kernel!)

┌─────────────────────────────────────────────────────────────────────────────────┐
│                           WINDOWS MACHINE                                        │
│                                                                                  │
│   ┌─────────────────────────────────────────────────────────────────────────┐   │
│   │                    WSL 2 (Lightweight VM)                                │   │
│   │                                                                          │   │
│   │   ┌──────────────────────────────────────────────────────────────────┐  │   │
│   │   │                   REAL Linux Kernel! 🐧                           │  │   │
│   │   │                                                                   │  │   │
│   │   │   Microsoft compiled Linux kernel version 5.x                    │  │   │
│   │   │   Provides: namespaces, cgroups, overlayfs                       │  │   │
│   │   │   Docker can run here!                                            │  │   │
│   │   │                                                                   │  │   │
│   │   └──────────────────────────────────────────────────────────────────┘  │   │
│   │                                                                          │   │
│   │   ┌──────────────────────────────────────────────────────────────────┐  │   │
│   │   │                   Ubuntu / Debian / etc.                          │  │   │
│   │   │                                                                   │  │   │
│   │   │   Linux distribution running on top of kernel                    │  │   │
│   │   │   Docker Engine installed here                                    │  │   │
│   │   │                                                                   │  │   │
│   │   └──────────────────────────────────────────────────────────────────┘  │   │
│   │                                                                          │   │
│   └─────────────────────────────────────────────────────────────────────────┘   │
│                                          │                                       │
│   ┌──────────────────────────────────────▼──────────────────────────────────┐   │
│   │                          Hyper-V                                         │   │
│   │                                                                          │   │
│   │   Windows virtualization technology                                      │   │
│   │   Creates the VM where WSL 2 runs                                       │   │
│   │                                                                          │   │
│   └──────────────────────────────────────────────────────────────────────────┘   │
│                                          │                                       │
│   ┌──────────────────────────────────────▼──────────────────────────────────┐   │
│   │                          Hardware                                        │   │
│   │                                                                          │   │
│   │   CPU must support virtualization (Intel VT-x / AMD-V)                  │   │
│   │                                                                          │   │
│   └──────────────────────────────────────────────────────────────────────────┘   │
│                                                                                  │
└─────────────────────────────────────────────────────────────────────────────────┘
```

---

## ⚙️ Hyper-V: What Is It?

```
Hyper-V = Windows Virtualization Technology

Two requirements:
1. HARDWARE support - CPU must have VT-x/AMD-V
2. SOFTWARE enabled - Windows feature turned ON

Check Hardware Support:
───────────────────────
Task Manager → Performance → CPU
Look for: "Virtualization: Enabled" ✅

Enable Hyper-V:
───────────────
Control Panel → Programs → Turn Windows features on/off
→ Check "Hyper-V" → Restart
```

---

## 📊 WSL 1 vs WSL 2

| Feature | WSL 1 | WSL 2 |
|---------|-------|-------|
| **Linux Kernel** | Translation layer | Real kernel! |
| **Docker support** | ❌ No | ✅ Yes |
| **File system speed** | Fast (Windows) | Slower (cross-FS) |
| **Memory** | Shared | Dedicated VM |
| **System calls** | Translated | Native |

**Docker requires WSL 2** - WSL 1 won't work!

---

## 🔄 Complete Layer Stack

```
┌─────────────────────────────────────────────────────────────────┐
│ Layer 1: YOUR APPLICATION                                        │
│          nginx, java, node, etc.                                 │
└──────────────────────────────┬──────────────────────────────────┘
                               │
┌──────────────────────────────▼──────────────────────────────────┐
│ Layer 2: CONTAINER                                               │
│          Isolated process with its own filesystem                │
└──────────────────────────────┬──────────────────────────────────┘
                               │
┌──────────────────────────────▼──────────────────────────────────┐
│ Layer 3: DOCKER ENGINE                                           │
│          dockerd → containerd → runc                            │
└──────────────────────────────┬──────────────────────────────────┘
                               │ System calls
┌──────────────────────────────▼──────────────────────────────────┐
│ Layer 4: LINUX KERNEL (in WSL 2)                                │
│          namespaces, cgroups, overlayfs                         │
└──────────────────────────────┬──────────────────────────────────┘
                               │
┌──────────────────────────────▼──────────────────────────────────┐
│ Layer 5: WSL 2 VM                                                │
│          Lightweight virtual machine                             │
└──────────────────────────────┬──────────────────────────────────┘
                               │
┌──────────────────────────────▼──────────────────────────────────┐
│ Layer 6: HYPER-V                                                 │
│          Windows virtualization layer                           │
└──────────────────────────────┬──────────────────────────────────┘
                               │
┌──────────────────────────────▼──────────────────────────────────┐
│ Layer 7: HARDWARE                                                │
│          CPU (VT-x enabled), RAM, Disk                          │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🛠️ Common WSL 2 Commands

```bash
# Check WSL version
wsl --version

# List installed distributions
wsl --list --verbose

# Set WSL 2 as default
wsl --set-default-version 2

# Restart WSL
wsl --shutdown

# Access WSL filesystem
\\wsl$\Ubuntu\home\...
```

---

## 🐳 Docker Desktop WSL 2 Integration

When you install Docker Desktop:

```
1. Installs Docker CLI in Windows
   └── C:\Program Files\Docker\Docker\resources\bin\docker.exe

2. Creates docker-desktop WSL distribution
   └── wsl --list shows: docker-desktop, docker-desktop-data

3. Runs Docker Engine inside WSL 2
   └── dockerd runs in docker-desktop distro

4. Shares /var/run/docker.sock
   └── All WSL distros can access Docker
```

---

## 🎬 Telugu Summary

```
Problem:
────────
Docker ki Linux kernel features kavali
Windows lo avi levu!

Solution:
─────────
WSL 2 = Windows lo Linux run cheyyochu
Real Linux kernel - Docker run avuthundi!

Layers:
───────
Your App → Container → Docker Engine → Linux Kernel → WSL 2 → Hyper-V → Hardware

Requirements:
─────────────
1. CPU: VT-x/AMD-V support (Task Manager check)
2. Windows: Hyper-V enabled
3. WSL 2: Installed and default
4. Docker Desktop: WSL 2 integration ON
```

---

## ✅ Verify Your Setup

```powershell
# 1. Check Hyper-V
Get-WindowsOptionalFeature -Online -FeatureName Microsoft-Hyper-V

# 2. Check WSL version
wsl --version

# 3. Check Docker WSL distributions
wsl --list --verbose

# Expected output:
#   NAME                   STATE           VERSION
# * docker-desktop         Running         2
#   docker-desktop-data    Running         2
#   Ubuntu                 Running         2
```

---

**Next: [03-engine-components.md](./03-engine-components.md)** 👉
